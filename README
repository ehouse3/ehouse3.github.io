Euan House | Notes for development

for TS compiling:
 need to compile all ts files with 
    'tsc home.ts --module ES6'
    'tsc token.ts --module ES6'
    'tsc board.ts --module ES6'

for testing/local hosting
 npm install -g http-server
 run
    'http-server -c-1'